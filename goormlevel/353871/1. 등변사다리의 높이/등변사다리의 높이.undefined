#include <iostream>
#include <cmath>

using namespace std;
int main() {
	int a, b, c;
	cin >> a >> b >> c;
	double diff_abs = abs(b - c);
  double result = pow(a, 2) - pow(diff_abs / 2.0, 2);
	int answer = (int)(result * 4);
	cout << answer;
}